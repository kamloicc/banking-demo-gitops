apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: banking-octopus-dev

resources:
  - ../../../base/account

labels:
  - pairs:
      app.kubernetes.io/environment: dev
    includeSelectors: true

images:
  - name: kamloicc.jfrog.io/banking-docker-local/banking-demo/account-service
    newTag: '#{Octopus.Action.Package[account-service].PackageVersion}'
